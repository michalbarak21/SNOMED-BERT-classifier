# CPU-only. Install torch from the CPU index:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
torch==2.8.0
transformers>=4.57
numpy==1.26.4
spacy>=3.8            # medspacy 1.3.1 requires >=3.8
medspacy==1.3.1
tensorboard>=2.20   # loss / accuracy logging
scikit-learn>=1.5   # macro-F1 and the per-class report
matplotlib>=3.8     # the report figures
